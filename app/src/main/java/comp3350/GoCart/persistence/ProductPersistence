package comp3350.gocart.persistence;

import java.util.List;

import copmp3350.objects.Product;

public interface ProductPersistence {
    List<Product> getProduct(Product currentProduct);

    //likely wont be needed
    Product insertProduct(Product currentProduct);
    Product updateProduct(Product currentProduct);
    void deleteProduct(Product currentProduct);
}